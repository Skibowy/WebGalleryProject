@model MongoWebGallery.Models.UserImagesViewModel

<div class="container image-container">
    <h2>Obrazy publiczne użytkownika @Model.UserName</h2>

    <div class="gallery-grid">
        @foreach (var image in Model.Images)
        {
            <div class="gallery-item">
                <a href="@Url.Action("Details", "Image", new { id = image.Id.ToString() })">
                    <img src="data:image/png;base64,@image.Base64Image" alt="@image.Name" />
                </a>
            </div>
        }
    </div>

    @if (!Model.Images.Any())
    {
        <p>Brak publicznych obrazów dla tego użytkownika.</p>
    }
</div>
